id: github_secrets_ci_cd
name: GitHub Secret Hunting - CI/CD Secrets
description: Suche nach Secrets in CI/CD Konfigurationen
category: code_repos
severity: critical
risk: critical
tags:
- github
- ci_cd
- secrets
- github_actions
- jenkins
- travis
platform_dorks:
  github:
  - path:.github/workflows (secret OR token OR key OR password)
  - path:.gitlab-ci.yml (password OR token OR secret)
  - filename:.travis.yml (encrypt OR env)
  - filename:Jenkinsfile (password OR credentials)
  - '"GITHUB_TOKEN" OR "TRAVIS_TOKEN" OR "CI_TOKEN"'
