description: Supports structured JSON output responses.
schema:
  type: object
  properties:
    response_format:
      type: object
      properties:
        type:
          type: string
          enum: [json_object, json_schema]
        json_schema:
          type: object
